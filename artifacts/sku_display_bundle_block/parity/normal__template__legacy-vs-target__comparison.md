# Parity: normal legacy vs target

- `nmId` сохранён без изменения: `210183919`, `210184534`, `210185771`.
- `CONFIG.comment` перенесён в `display_name` без дополнительного enrichment.
- `CONFIG.group` сохранён как `group`.
- `CONFIG.active` сохранён как `enabled`, включая inactive SKU `210185771`.
- Порядок строк сохранён через `display_order = 1..3` по row order legacy sample.
