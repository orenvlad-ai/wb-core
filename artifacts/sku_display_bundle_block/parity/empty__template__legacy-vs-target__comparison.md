# Parity: empty legacy vs target

- Legacy sample не содержит ни одной строки `CONFIG`.
- Target возвращает `kind = "empty"` и `count = 0`.
- Empty-case честный и не требует переноса полного `CONFIG` или registry-слоя.
