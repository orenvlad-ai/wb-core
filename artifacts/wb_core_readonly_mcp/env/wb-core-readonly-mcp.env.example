# Runtime-only env for wb-core-readonly-mcp.service.
# Store the real token outside Git at:
# /opt/wb-core-readonly-mcp/env/wb-core-readonly-mcp.env
#
# Do not commit the real value and do not print it in logs/handoffs.
WB_CORE_READONLY_MCP_TOKEN=replace-with-runtime-only-token
