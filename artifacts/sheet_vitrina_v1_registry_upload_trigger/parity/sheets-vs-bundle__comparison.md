# Sheet Vitrina V1 Registry Upload Trigger: parity

- `CONFIG!A:E` materialize-ит `config_v2`.
- `METRICS!A:J` materialize-ит `metrics_v2`.
- `FORMULAS!A:C` materialize-ит `formulas_v2`.
- `CONFIG!I2` хранит `endpoint_url` и не входит в upload bundle.
- `CONFIG!I3:I7` фиксирует последний upload result для оператора и не входит в upload bundle.
