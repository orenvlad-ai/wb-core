# Cogs By Group Empty Comparison

- Legacy sample не содержит ни одной COGS row для requested `nmId`.
- Target возвращает `kind = "empty"`.
- `items = []`, `count = 0`.
- `date_from/date_to` сохранены без изменений.

Вывод: empty/no-row parity подтверждена.
