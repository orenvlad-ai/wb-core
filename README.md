# wb-core

`wb-core` — target-core репозиторий для controlled sidecar migration проекта «Динамики и корреляции» на WB.

Legacy-репозитории остаются рабочими, но считаются maintenance-only:
- `wb-table-audit`
- `wb-ai-research`
- `wb-web-bot`

Новая разработка и canonical migration state фиксируются только в `wb-core`.

## Текущий checkpoint `main`

`origin/main` уже не является foundation-only состоянием. В `main` смёржены bounded implementation-блоки для:
- `web-source` и `official-api` snapshot family;
- `rule-based` модулей;
- `table-facing` и `projection` read-side;
- `registry` pilot line и первый upload bundle path;
- `wide matrix`, `delivery`, archived legacy sheet-side/export contour и текущую web-витрину.

Подтверждённый main-confirmed contour:

`sku_display -> table_projection -> registry_pilot -> wide_matrix -> delivery -> sheet_scaffold`

В `main` также уже есть:
- `Отчёты -> Финотчёт ВБ` читает official weekly finance reports через единый warehouse-domain resolver `Себестоимость WB наша`: до `2026-07-01` — exact same-`nmId` retrospective projection значения на `2026-07-01`, с этой даты — exact canonical daily WB WAC соответствующей operation date. Legacy `COST_PRICE`, cross-SKU/average и silent-zero fallback запрещены; coverage/quality stay explicit. Repo-owned hourly finance timer and bounded historical recalculation preserve their guarded fingerprint/backup/atomic/non-target contract;
- legacy/export write bridge, presentation pass и compact v3 bootstrap для bound Google Sheet, но этот Google Sheets/GAS contour теперь `ARCHIVED / DO NOT USE` и не является active runtime/update/write/verify target;
- отдельный bounded `COST_PRICE` server contour: sibling HTTP upload path, server-side authoritative storage seam вне compact registry bundle и read-side overlay в refresh contour для `cost_price_rub`, `total_proxy_profit_rub`, `proxy_margin_pct_total`;
- bounded promo line в двух шагах:
  - `promo_xlsx_collector_block` как thin browser-capture precursor для promo XLSX + metadata sidecar;
  - `promo_live_source_wiring_block` как current live wiring обратно в `sheet_vitrina_v1` refresh/runtime/read-side contour для `promo_by_price` и promo-backed numeric rows;
- вкладка `Отзывы` в active website/operator contour: read-only WB feedbacks load/filter table со строгими server-side `date_from/date_to/stars/is_answered` фильтрами, official WB review tags/chips в таблице/Excel/AI input, bounded 62-day feedback date picker independent from ready-snapshot dates and defaulting to the current server/business week (`D-6..D`) when no fresh v3/TTL same-day manual range exists, chunked `take/skip` загрузкой без тихого row cap, diagnostic meta, Excel export текущей таблицы, resizable columns, bounded AI-assisted разбор через server-side prompt+model config/OpenAI route, подраздел `Жалобы` над runtime-журналом жалоб and `Авто-жалобы` for runtime-stored daily schedules; operator UI показывает статус жалобы по `feedback_id`, поверх отсутствующего journal record показывает последнюю runtime submit-attempt overlay вместо тихого `—`, даёт выбрать только non-journaled rows после AI-разбора и запускает protected async `submit-selected` job через тот же guarded Seller Portal submit/resolver path с hard cap `5`, exact/actionable gates and event log. `Авто-жалобы` stores editable daily schedules in runtime state, defaults to `Asia/Yekaterinburg`, returns canonical persisted rows after save while treating lifecycle fields such as `last_success_at`/`last_run_id` as server-owned, disables `Запустить сейчас` for unsaved/dirty rows, returns structured `schedule_not_found` for stale ids, computes first-run 24h windows without overlap and recurring `last_success_at..now` windows with 24h overlap, considers only 1–2★ feedbacks, reuses the same AI `yes/review` semantics and guarded submit path, classifies existing complaint-journal/prior-attempt candidates by confirmation evidence instead of a coarse `existing_journal_feedback_id` bucket, retries `error` journal records through the guarded lane, surfaces journal-only/unconfirmed records as explicit probe-required blockers, creates observable sanitized run reports for run-now/tick, updates schedule `last_run_id/status/stats` as runs are queued/running/finished and makes `Посмотреть лог` show either run details, explicit still-running guidance, or an explicit empty-log state; status sync по `Мои жалобы` не зависит от Search Analytics preflight для чтения жалоб, открывает `Ждут ответа`/`Есть ответ` прямыми status URL и глубоко сканирует bounded pagination/scroll-ом, по умолчанию сверяет только runtime records со статусом `Ждёт ответа` и обновляет журнал только по direct-id или strict strong-composite evidence; shared Seller Portal resolver treats `Есть ответ` / `Ждут ответа` as first-class status tabs, uses date/star diagnostics, safe WB article/nmId search, disabled/already-complained action diagnostics and bounded virtualized scroll before declaring a row not actionable; bounded complaint batch reports split not-submitted rows into explicit reason groups such as already-in-journal, exact-match unsafe, true DOM unavailability, filter/tab/scroll issues, disabled/already-complained, description persistence and submit-unconfirmed instead of generic `other`; AI-разметка не является accepted truth/ЕБД persistence, а неопределённые submit-попытки проверяются отдельными read-only confirmation/detail-network probe runners с direct-id или strict strong-composite proof;
- вкладка `Реклама` в active website/operator contour: SKU-first table over active `registry_upload_config_v2` nm_id universe with nomenclature enrichment, WB Promotion API reverse index `nm_id -> campaigns[] -> placements[]`, drawer preserving `advert_id`, campaign name/status/payment/bid type, placement, current/min/recommended bids and campaign/SKU aggregate stats, plus guarded backend-only bid change workflow `preview -> explicit commit -> audit -> delayed refresh` for exactly one `nm_id + advert_id + placement`; write route is disabled unless `SHEET_VITRINA_ADS_WRITE_ENABLED=1`, validates min/safety thresholds and current-bid freshness, writes runtime JSONL audit, never performs bulk changes, auto-bidding or direct frontend WB/PATCH writes;
- раздел `Поставки` получил внутреннюю навигацию `Расчёты` / `Wildberries` / `ФФ` / `Счёт CNY` / `От поставщика`: текущие factory-order и WB regional calculators остаются в `Расчёты`, `ФФ` содержит `Услуги ФФ` and `Остатки ФФ`, `Счёт CNY` ведёт server-side ledger валютного CNY-счёта with opening balance, VTB conversion PDF parsing, Decimal moving-average replay/backfill and supplier-order effective CNY rate calculation, а `От поставщика` открывает server-owned supplier invoice registry с XLSX parser (`openpyxl`), runtime SQLite header/line state, filesystem storage оригинальных invoice-файлов under runtime dir, protected parse/create/list/detail/patch/download API routes, editable product/extra lines, required `Плановая дата отгрузки / Planned shipment date` (`shipment_date`) plus nullable fact dates `actual_shipment_date` and `actual_ff_acceptance_date`, exact barcode-only matching against active server-side nomenclature without type/model/alias/compatibility fallback and optional supplier-only account boundary. Authenticated supplier получает отдельные server-side allowlisted parse/list/detail/write responses и производственный HTML без внутренних costs, price conformity, CNY/банковских данных, document IDs/paths/downloads; сохранённые shipment invoice/contract и любые order/financial/logistics files для supplier закрыты единым `403`, тогда как `admin`/`operator`/`supply_operator` сохраняют полный read/document contour. Supplier deadline badge вычисляется из canonical business date: `10+` дней green, `0–9` yellow, прошедшая дата red, а occurred factual shipment показывает `Отгружено`. В карточке заказа операторский inner-tab `Финансовые документы` добавляет PDF upload/parser/storage для логистических КП, счетов логиста, ДТ, CNY conversion/payment documents and VTB bank statements for anchored bank-fee preview/confirm import, server-owned financial document/expense-line rows, CBR USD/RUB benchmark seam, quote-vs-invoice reviewable matching, exact CNY-ledger payment/fee cost fields and logistics/customs/VAT/efficiency summary without SKU cost allocation or WB mutations. Factory-order keeps manual Excel `Товары в пути от фабрики` as default input, but can use the supplier registry as a mutually exclusive read-only source; supplier shipment rows enter coverage only after deterministic `nmId` matching and current planned `shipment_date + 30 days` acceptance-date conversion. Shared `Остатки ФФ` now has source selection: manual Excel remains default, `1С / Фулфилмент` reads server-owned materialized `onec_FF_STOCK_qty` from the existing `onec_stocks` / `onec_product_capital` / `FF_STOCK` contour, and source `Остатки ФФ` reads computed balances from the server-owned ФФ quantity ledger; `onec_total_qty` is never used as `stock_ff`. Factory-order demand now uses availability-adjusted `orderCount` estimation: `sales_avg_period_days` collects valid trading days inside a bounded lookup window instead of blindly averaging zero/low out-of-stock tail calendar days. WB regional `Поставка на Wildberries` no longer splits district demand by current stock share as primary mode: total SKU demand remains `orderCount`, district shares are restored by a bounded ladder `full clean days -> partial district-day observations -> SKU group prior -> global prior -> seed floor`, and each district now has its own `Доставка, дней` input whose value means lag until goods are available on WB (delivery + acceptance/sorting/appearance on sale). Dirty cells no longer discard other districts' valid observations, `0 -> 0` is no-signal rather than demand evidence, current-stock-share fallback is not a normal path, seed floor is explicitly a one-box exploration fallback rather than a demand-based share, and limited `stock_ff` allocation prioritizes marginal saved units before coverage-days tie-break. Operator result diagnostics are compact/human-readable in Russian; selected-district summary/download surfaces show only districts included in methodology, while excluded districts stay available in selector/options and direct excluded XLSX download returns a controlled JSON error. Individual district XLSX downloads keep stable ASCII route filenames and exclude `Дефицит`; `Скачать все рекомендации` builds one atomic run-identified ZIP where every destination folder has the same unique prefix as its operator XLSX and canonical-template `Баркод / Количество` WB-upload XLSX, with exact text barcodes, duplicate aggregation, total reconciliation and fail-closed all-or-nothing validation;
- системный раздел `Инструкции` добавлен в правую группу общего operator shell рядом с `Настройки`/`Выйти`: он публикует Git-tracked structured web-native справочник, а не CMS или DOCX/PDF. Route `GET /sheet-vitrina-v1/instructions` защищён отдельной server-owned section capability `instructions`; admin получает её через существующую административную семантику, а для остальных пользователей она явно управляется через `Настройки -> Пользователи`. Никакие статья, capability truth или supplier access не выдаются из browser/localStorage, и nginx publication не делает route public;
- supplier shipment `order_status` is a server-derived compatibility cache, not separately editable truth: occurred factual shipment/FF-acceptance dates plus canonical business today produce `На производстве`, `В пути с DD.MM.YYYY` or `Принято на ФФ с DD.MM.YYYY` across API/UI/downstream consumers; retained archived MCP compatibility reads the same resolver only when explicitly exercised. Exact audited legacy signal `legacy_ff_accepted_without_date` adds only `Принято на ФФ · дата неизвестна`: it leaves factual dates null, excludes the shipment from factory/WIP and never creates FF movement/stock/cost/capital. Ordinary Save cannot change this signal. A changed `actual_shipment_date` starts one persisted audited correction with real progress, verified disposable candidate, bounded canonical rebuild from cutover when materialized, semantic exact fingerprint, fresh online backup, atomic apply/in-place rollback and zero-change repeat; unrelated live-row activity is excluded from human approval while protected source collateral remains transaction-local before/after equal. A required full rebuild may roll forward canonical daily rows outside the target SKU closure only when every exact row delta and before/after canonical digest is bound into the human fingerprint; any extra output rolls back. The same guarded runner can truthfully adopt an already-correct field and atomically activate/reverse the exact historical signal, including an exact zero-change repeat of an already audited activation. `apps/supplier_shipment_publication_chain.py` binds this result to the exact ready-snapshot canonical publication and one chain fingerprint so the operator does not need a later manual `Загрузить`; no extra recalculate button exists. `Поставки -> Реестр поставок` renders the same derived display as a compact status badge in every shipment-column header;
- внутренние `Поставки -> От поставщика -> Реестр заказов / Документы` получили независимые browser-local choosers столбцов и видимый по умолчанию server-derived статус `Распределение расходов`. Статус использует только canonical `supplier_cost_breakdown` document components/conservation/fingerprints и зелёный order gate требует актуальной `expenses_complete` certification; supplier-safe JSON/DOM не получает эти поля. Пакет логистов сохраняет exact contract + все VTB transfer applications + все bank-control statements и прежний truthful partial/error receipt. Бухгалтерский пакет содержит только Invoice + contract + все активные ДТ + отдельную openpyxl XLSX-расшифровку каждой ДТ, но отдаётся только при 100% трёхсторонней сверке `DT annex rows -> linked Invoice product lines -> current-order server nomenclature`: product/DT row counts, Invoice/header/DT totals, every owner quantity, nmID/name/barcode, workbook and ZIP manifest size/SHA-256 readback должны пройти полностью. Box-31 projection `supplier_customs_annex_items_v2` поддерживает single-position multi-row и повторяющиеся multi-position `Товар № N` tables across page breaks, не меняя canonical goods/weight/payment aggregates. DT-only policy `supplier_customs_dt_annex_strict_reconciliation_v2` сохраняет exact barcode/full model-set priority и разрешает только unique complete bounded graph solution по точной серии, non-empty model-key containment и количествам; `14-13-13PRO` сопоставляется с более широким compatible model set только при единственном полном решении текущего заказа. Любой missing/ambiguous/unmatched/quantity/identity blocker возвращает controlled JSON `409` без ZIP/browser download; успешный accounting receipt всегда `complete + requires_review=false`. Supplier Invoice identity остаётся barcode-only, package assembly reparses сохранённые ДТ read-only и не пишет business state;
- `Поставки -> Wildberries` добавлен как read-only WB API / FBW Supplies registry рядом с `Расчёты` и `От поставщика`: protected cached API routes, `WB_API_TOKEN` official Supplies boundary, runtime SQLite cache/state/run/warehouse tables, ordinary `Обновить поставки` is incremental/latest-window sync with raw hash comparison followed by a separate UI-orchestrated Seller Portal `supply/cost` network-json enrichment attempt for missing transit costs and idempotent internal `ФФ -> Остатки ФФ` writeoffs for statuses `3/4/5/6` by goods composition, guarded by an explicit WB auto-writeoff checkpoint with baseline-known cache/source/supply keys, ledger activation/opening balance and no-negative-balance checks so historical WB cache/backfill cannot silently create negative ФФ balances, targeted active-status reconciliation for `statusIDs=[1,2,3,4]`, hard-delete of confirmed removed active supplies from the working cache, preservation of accepted/historical rows absent from latest windows, changed-only detail/goods enrichment for new/changed rows plus active detail/goods refresh, old unknown-quantity historical rows are retried only by explicit missing-critical enrichment, separate resumable full backfill route/button/runner loads all API-available history by `limit/offset`, quality-aware row upsert preserves cached detail/goods/package evidence across list-only sync, cache re-normalization can rebuild rows from stored raw evidence, detail route and row-click UI expose normalized goods composition (`nmID`/barcode/vendorCode/quantities) for a supply, `boxTypeID=0/virtualTypeID=5` renders as `Допринято` instead of `Тип 0` and is skipped by stock ledger writeoff, non-transit accepted free rows show `0 ₽`, official-unresolved transit cost stays `— с транзитом` until the supplemental Seller Portal stage stores normalized `seller_portal_browser` facts and fills effective display without overwriting official `cost_total`, field-level normalization for route/warehouse, quantity/packed/accepted and cost evidence, search/warehouse/status/size filters without status overrides, federal-district presets live inside the `Склад` dropdown, checkbox multi-status filter with `Все`/`Активные`/`Сброс`, persisted filters after refresh, supply-date sort before pagination with empty-date/`Не запланировано` rows at the bottom, current-year dates without year and non-current-year dates with year, default `Размер поставки = Основные от 250 шт` only for numeric `quantity_for_size_filter >= 250`, compact WB-like table and pagination `20/50/100`. Seller Portal/session failures are transit-cost warnings and do not invalidate successful official sync; Acceptance Expenses Reports API was checked as a transit-cost candidate and returned no target rows/amounts for the tested target windows, so this supplemental source remains separate from official sync. Отдельный repo-owned CLI жёстко ограничен `supply_id=40561872`: его v2 contour читает goods только из cache и обходит ровно два подтверждённых ordinary blocker — baseline `wb_supply_before_auto_writeoff_checkpoint` и pre-activation `wb_supply_before_ledger_activation` — после exact fingerprinted dry-run, integrity-checked backup и atomic recheck activation/checkpoint/nomenclature/balances/totals; обычные sync/backfill/detail и pre-activation guards не изменены. Этот cache не является ЕБД metric truth, не пишет Google Sheets/GAS and does not add WB create/update/delete mutations;
- `Себестоимость WB наша` и Proxy 3 являются direct read projections active functional engine на всей активной временной поверхности. До `2026-07-01` общий resolver проецирует exact same-`nmId` cost и effective settings с `2026-07-01`; после границы использует exact-date cost/settings. Proxy 3 никогда не подменяется Proxy 2: expected buyout revenue/quantity, retained share and canonical WB WAC считаются по применимой temporal policy; margin denominator is expected buyout revenue, TOTAL is ratio of aggregates, missing operands stay NULL. Default effective version is 91% buyout, 44% expenses and 56% retained. Global vitrina refresh only reads materialized warehouse/cost state; separate bounded hourly/manual WB sync owns external source refresh;
- `Поставки -> Расчёты` использует calculation-only overlay `Учесть WB-поставки` поверх выбранного source `Остатки ФФ` (manual Excel, `1С / Фулфилмент` или server ledger). Backend берёт только eligible statuses `3/4/6`, operational date and `nmId -> quantity`, revalidates active SKU and maps the planned/target destination (never actual/transit) into the supply planning contract. Central destinations require exact warehouseID registry mapping into North/East/South and fail closed when unknown; other federal districts retain the existing mapping. For manual/1C sources selected supplies reduce free `stock_ff`; for `ff_stock_ledger` they are not deducted twice, but still enter only their exact destination zone projection. Status `2` is a slot reservation and status `5` is already stock evidence, so both are skipped with diagnostics. The overlay remains read-only calculation evidence without WB mutation or metric truth promotion;
- `Поставки -> Расчёты -> Подобрать склады WB` является read-only storage-only assistant поверх результата `v2_planning_zones`: общий reporting key `central` не меняется, а supply calculation показывает `ЦФО Север / Восток / Юг` без `ЦФО Запад`. Central warehouse registry привязан к exact `warehouseID`; WB `acceptance/options` для каждого ШК/quantity даёт SKU-dependent evidence, после чего backend fail-closed исключает СЦ/СГТ/спецсклады, `canBox=false`, inactive, partial, blocked and unclassified warehouses. Dates use only box `boxTypeID=1/2`, `allowUnload=true`, coefficient `0/1` and unique calendar days. UI receives reason codes/ranking evidence and renders one selected direction in a separate full-width card without any WB create/booking/mutation;
- `Отчёты -> Отчёт по остаткам` в operator UI теперь является manual-calculate full active-SKU sortable table over persisted ready snapshot truth: controls for SKU, `sales_avg_period_days` and browser-local column visibility render first, `/v1/sheet-vitrina-v1/stock-report` is fetched only after `Рассчитать`, stock and `promo_participation` come from `DATA_VITRINA/yesterday_closed`, `sales_avg_period_days` defaults to `14` and uses the shared availability-adjusted demand helper over persisted `sales_funnel_history`, total/district days-left stay numeric/null, district расход is calculated only from positive depletion between consecutive persisted ready snapshots, and after `Акция` the table shows the product-cycle columns `на произв.`, `в пути Китай`, `ост. ФФ`, `поставки ВБ`, `ост. ВБ`. `на произв.` and `в пути Китай` read current supplier shipment registry product lines by `internal_nm_id -> qty` for statuses `production` / `in_transit`, `ост. ФФ` reads server-owned `ff_stock_ledger` current balances, `поставки ВБ` reads current WB supplies cache/goods composition with only statuses `1/2/5` (`Не запланировано`/`Запланировано`/`Принято`) excluded, and `ост. ВБ` remains the existing `stock_total` WB stock value. The table renders an `Итого` row above SKU rows, uses horizontal scroll with sticky `SKU`, and preserves the current horizontal position when a header sort rebuilds the table for ascending or descending order; it does not revive Google Sheets/GAS or browser-side data truth;
- все live Seller Portal automation runners в этом contour используют единый single-flight lock `/opt/wb-core-runtime/state/seller_portal_automation.lock.json`, canonical bot storage state `/opt/wb-web-bot/storage_state.json` или явный `SELLER_PORTAL_STORAGE_STATE_PATH`, без implicit local Mac fallback в EU runtime. Status sync, submit/batch, auto-complaints tick/run-now, scout/probe/dry-run/confirmation/detail/relogin and future parser/export jobs must not run parallel Playwright sessions: a busy lock returns controlled `seller_portal_automation_busy` metadata without secrets. Route-specific checks are explicit (`seller_portal_base`, `feedbacks_list`, `complaints_pending`, `complaints_answered`, supplier contexts), and `Мои жалобы` jobs warm up Seller Portal base/UI context before deep status URLs with deterministic settle/cooldown rather than stealth/evasion behavior;
- первый bounded end-to-end MVP history `prepare -> upload -> refresh -> load` сохранён как archive/migration reference; current active contour = website/operator `sheet_vitrina_v1` и public web-витрина без Google Sheets completion blocker;
- evidence и module docs по этим шагам.

Главные незакрытые gaps на текущем `main`:
- full legacy parity по всем историческим registry rows и metric sections;
- full legacy parity beyond current `102`-row uploaded metric dictionary и beyond current server-driven two-day read-side без поломки текущего contour;
- repo-owned hosted runtime deploy/probe contract теперь materialized в repo, including managed public-route allowlist publishing for current wb-core routes and simple app-level WebCore auth over public/operator HTML+API surfaces; active/current live hosted runtime target = `wb-core-eu-root` / `89.191.226.88` / `/opt/wb-core-runtime/state`, current public base = `https://api.selleros.pro`, and managed nginx must publish both `89.191.226.88` and `api.selleros.pro` with `443 ssl`; this HTTPS/domain publication is a hard current-live invariant, so future IP-only or HTTP-only target drift fails locally before deploy/apply-nginx mutation; legacy `selleros-root` / `178.72.152.177` target is rollback-only/read-only evidence and routine deploy/apply-nginx/restart/update/GC writes fail fast unless the explicit emergency rollback override is set;
- WebCore Data MCP implementation is retained only as an archived read-only compatibility gateway on `127.0.0.1:8766`. It is not a normal prompt/source/acquisition path and its absence is never a blocker; current production evidence/data is read from the canonical server-side stores/documents through the current SSH/query-only protocol. Historical OAuth routes, compact tools, worker/deadline/result caps, audit and redaction contracts remain documented solely for compatibility maintenance;
- promo artifact retention встроен в hosted refresh: after normalized promo archive + ready snapshot persistence refresh runs bounded `promo_refresh_light_gc_v1`, protects current/unknown/replay-critical artifacts and surfaces GC summary in refresh diagnostics/job log;
- окончательное решение по судьбе `AI_EXPORT` как compatibility contract или прямой замене server-side contract.

## Что repo уже содержит

- `packages/contracts`, `packages/application`, `packages/adapters` с живыми bounded modules;
- `apps/` с smoke runners; legacy live sheet-side runners сохранены как archived fail-fast guards;
- `artifacts/` с fixture/parity/evidence по смёрженным блокам;
- `registry/pilot_bundle/` как pilot registry line для новой витрины;
- `artifacts/registry_upload_bundle_v1/` как первый artifact-backed upload path для V2-реестров;
- `artifacts/registry_upload_file_backed_service/` как локальный file-backed receiver для registry upload path;
- `artifacts/registry_upload_db_backed_runtime/` как локальный DB-backed runtime layer для registry upload path;
- `artifacts/registry_upload_http_entrypoint/` как первый live HTTP entrypoint для registry upload path;
- `artifacts/sheet_vitrina_v1_registry_upload_trigger/`, `artifacts/sheet_vitrina_v1_registry_seed_v3_bootstrap/` и `artifacts/sheet_vitrina_v1_mvp_end_to_end/` как archived/migration evidence для бывшего sheet-side contour;
- `packages/application/cost_price_upload.py` и `packages/contracts/cost_price_upload.py` как отдельный authoritative upload contract для `COST_PRICE`, который затем подключается server-side в `sheet_vitrina_v1` refresh/read contour;
- `gas/sheet_vitrina_v1/` и `.clasp.json` для legacy bound sheet-side/export wiring; GAS содержит archive guard, не является active production/update/write/verify target и не должен использоваться как Codex completion path;
- `AGENTS.md` как короткий самодостаточный execution/governance context для Codex и нового ChatGPT Work Project;
- `docs/modules/` как канонический модульный reference;
- `migration/` как канонический слой migration contracts и implementation notes.

## Короткая хронология `main`

1. Foundation и control/docs слой.
2. Перенос `web-source` и `official-api` snapshot-блоков.
3. Rule-based блоки и doc-sync по канонической модульной документации.
4. Table/registry/wide/sheet line: `sku_display`, `table_projection`, `registry_pilot`, `wide_matrix`, `delivery`, `sheet_scaffold`, `live write`, `presentation`.
5. Upload contract для V2-реестров.
6. Первый artifact-backed upload bundle и local validator для V2-реестров.
7. Первый file-backed upload service, current-marker и structured upload result для V2-реестров.
8. Первый DB-backed runtime ingest и current server-side truth для V2-реестров.
9. Первый тонкий live HTTP entrypoint для registry upload path.
10. Первый operator-facing sheet-side trigger для отправки `CONFIG / METRICS / FORMULAS` в уже materialized entrypoint.
11. Compact v3 bootstrap для `CONFIG / METRICS / FORMULAS` с сохранением service/status block.
12. Первый bounded end-to-end MVP, где expanded MVP-safe registry seed, upload и load `DATA_VITRINA` уже работают в одном контуре.
14. Отдельный bounded contour для `COST_PRICE`: `COST_PRICE` sheet-side prepare/upload, sibling `POST /v1/cost-price/upload` и separate authoritative dataset в том же runtime/app boundary.
15. Server-side read-side integration `COST_PRICE` в `sheet_vitrina_v1`: authoritative resolution по `group + max(effective_from <= slot_date)`, truthful `STATUS.cost_price[*]` и operator-facing derived metrics `total_proxy_profit_rub` / `proxy_margin_pct_total`.
16. Первый repo-owned bounded `promo_xlsx_collector_block`: canonical hydration/modal/drawer seams, truthful metadata sidecar, workbook inspection и bounded live integration smoke поверх existing seller session reuse path.
17. Bounded live wiring `promo_live_source_wiring_block`: `promo_by_price` больше не blocked gap, а current server-owned source seam внутри existing `refresh -> runtime -> STATUS/DATA_VITRINA` contour с accepted snapshot preservation и truthful low-confidence cross-year handling.
18. Legacy Google Sheets/GAS contour переведён в `ARCHIVED / DO NOT USE`; current contour = hosted website/operator/web-vitrina runtime.
19. В active `/sheet-vitrina-v1/vitrina` добавлены `Отзывы` с official WB review tags/chips, подразделы `Жалобы` и `Авто-жалобы`, runtime-журналом/deep status sync, compact supply/report/research operator surfaces and bounded AI-assisted feedback review over OpenAI with server-side prompt/model config, live model discovery, strict feedback period/star filters, Excel export and resizable feedback columns; protected operator-selected complaint submit запускает только async guarded backend job and exposes per-row attempt results, while auto-complaints schedules live in runtime state and are triggered by a repo-owned idempotent tick.
20. WebCore public/operator surface получил simple app-level auth with login/logout session cookie and env-provided username/password hash/session secret; plaintext owner credentials не являются repo state.
22. Seller Portal automation contour получил repo-owned single-flight policy: one lock for all browser runners, canonical EU bot storage-state path, route-specific session capability blockers and controlled public busy responses for status/submit/auto-complaints routes.
23. `Поставки -> От поставщика` добавлен как supplier invoice registry: protected API/UI, XLSX parser, runtime storage and optional supplier-only role isolation.
24. `Настройки -> Справочники -> Номенклатура` хранит server-owned SKU/nmID/WB reference для future FBW planning: primary `barcode`, all known `barcodes`, `vendor_code`, WB title/subject/updatedAt, source/status/sync diagnostics, manual barcode override priority, separate `is_hidden` visibility semantics outside operational `is_active`, read-only WB Content `POST /content/v2/get/cards/list` cursor sync through canonical `WB_API_TOKEN`, protected sync matching by `nm_id -> barcode -> vendorCode`, managed server-owned `Группы SKU` aliases for vendorCode auto-detection, XLSX import/export reference columns and readiness summary without Google Sheets/GAS/localStorage truth or WB mutations.
25. `Поставки -> Расчёты -> Заказ на фабрике` исправил inbound coverage на окно `report_date..full target_window` and can optionally use supplier registry rows in statuses `production`/`in_transit` as `Товары в пути от фабрики`, while manual Excel remains the default source and `accepted_ff` supplier orders are excluded to avoid double-counting with ФФ stock.
26. `Поставки -> Wildberries` добавлен как read-only WB/FBW supplies registry: official WB Supplies API adapter, server cache/incremental sync/full backfill/status routes, runtime SQLite persistence with run progress, compact operator table, persisted checkbox multi-status filters, targeted active refresh/reconciliation, hard-delete for confirmed removed active rows, historical accepted-row preservation, route/warehouse/quantity/cost evidence normalization, numeric size filters, clickable supply-date sorting with empty-date rows at the bottom, year-aware date display, pagination without WB mutations, and a unified `Обновить поставки` UI action that runs official sync first and then a separate Seller Portal browser/network-json enrichment for missing transit cost while preserving official/source provenance.
27. `Поставки -> Расчёты -> Поставка на Wildberries` использует отдельный planning-zone contract: `ЦФО Север`, `ЦФО Восток`, `ЦФО Юг`, `СЗФО`, `ПФО`, `УФО`, `ЮФО/СКФО`, `ДВФО/СФО`; `ЦФО Запад` отсутствует, а canonical federal key `central` остаётся совместимым вне этого модуля. `stocks-report/wb-warehouses` сохраняет warehouseID/name до exact Central zone mapping; current and historical zone depletion feed the existing full/partial/group/global demand ladder. Target stock minus current zone stock and selected in-transit supply produces a full box-rounded shortage; bounded FF quantity is allocated deterministically by real shortage, never negative or above availability, with full/allocated/unfulfilled diagnostics. Payload `v2_planning_zones` reads old persisted results only as preserved legacy snapshots and requires authoritative recalculation instead of inventing a three-way split. District XLSX/ZIP names use planning-zone stems.
28. `Поставки -> Расчёты -> Остатки ФФ` получил альтернативный source `1С / Фулфилмент`: check JSON + проверочный XLSX в формате manual template, calculation source `onec_ff_stock`, server-side coverage diagnostics и reuse existing materialized 1C `FF_STOCK` metric without creating a new 1C source-of-truth layer.
29. `Поставки -> Расчёты -> Учесть WB-поставки` добавлен как bounded overlay поверх `Остатки ФФ`: calculation selector/options use only statuses `3/4/6`, while `1/2/5` and `Допринято` are skipped. Manual Excel and `1С / Фулфилмент` keep the old transfer behavior where selected WB supplies reduce free ФФ and add in-window qty to factory `ФФ -> WB` / mapped regional projection; source `Остатки ФФ` / `ff_stock_ledger` keeps ledger `stock_ff` unchanged and uses selected WB supplies only as inbound/projection evidence.
30. `Поставки -> От поставщика -> Финансовые документы` добавлен как operator-only MVP financial contour по supplier order: real PDF upload, deterministic text-layer parser for logistics quote/logistics invoices/customs declaration, runtime SQLite financial documents/expense lines, original PDF download, CBR benchmark seam and compact logistics/customs/VAT/efficiency/per-unit summary with reviewable quote-vs-invoice matching.
31. `Поставки -> Реестр поставок` добавлен как protected operator matrix: shipments are columns, grouped metrics are rows, values come from existing supplier orders and financial documents, known КП gaps render short reason states (`нет КП`, `нет в КП`, `ошибка парсинга КП`, `нет стоимости груза в КП`, `курс не подтверждён`/`ждём счета`) while unknown/not-applicable values still render as `—`; raw КП metrics stay in the logistics quote block, КП-base normalized rows are separated from fact/ДТ-base normalized rows, cargo physics uses parsed packing-list facts, exact product cost uses matched CNY-ledger supplier payments rather than customs value, exact landed unit cost is colored by persisted operator-owned expense completeness, logistics-dependent normalized fact rows render yellow until expenses are marked complete, section headers and metric labels stay sticky during horizontal scroll, invoice headers open the shared supplier order detail, and MVP comparison lets the operator upload one temporary logistics КП PDF, select one shipment column and render КП vs shipment fact rows without saving that КП as a shipment/financial document. SKU cost allocation and parallel DB remain out of scope.
32. `Реклама` добавлена как SKU-first operator section over WB Promotion API: active nm_id table, SKU drawer with campaign/placement bids and metrics, guarded backend-only one-row bid preview/commit/audit flow, env-gated live writes and explicit no-bulk/no-auto-bidding boundary.
33. `Поставки -> Расчёты -> Подобрать склады WB` exposes only direct, active, full-storage warehouses allowed by WB for every required barcode and box quantity. The centralized Central registry keeps Тверь; Владимир/Рязань; Коледино/Тула/Воронеж recommendable, while Электросталь/Котовск remain exact historical East mappings but blocked for new recommendations. СЦ, СГТ, Питание, Горючее, Шины, partial barcode coverage, inactive/unknown IDs and wrong package types remain diagnostic-only and never enter the manager table; unique chronological available/free dates and explicit blocker codes are backend-owned.
34. `Поставки -> От поставщика` получил nullable manual `approx_yuan_rate` and advisory `Ориентировочная себестоимость на ФФ, ₽/шт = ((invoice_amount_total * approx_yuan_rate) + factual_supply_expenses_rub) / product_qty_total`; фактические расходы берутся только из persisted financial documents/expense lines, КП не считается фактом, а значение не участвует в `cost_price`, 1С, WB/web-vitrina metrics, factory-order calculations or final бухгалтерская себестоимость.
35. `Поставки -> Счёт CNY` добавлен как server-side валютный ledger: canonical CNY documents/operations/replay state in SQLite, VTB conversion purchase parser, opening balance support, moving-average account rate calculation, automatic deterministic replay/backfill after currency document uploads and supplier CNY payments, supplier order fields `Курс CNY по счёту` / payment RUB cost / bank fees / CNY status, and public/operator routes under `/v1/sheet-vitrina-v1/supply/cny-account...`; manual `approx_yuan_rate` remains advisory and is not overwritten.
36. `Поставки -> От поставщика -> Финансовые документы` поддерживает VTB bank statement fee import: upload creates a scoped `bank_fee_statement` preview, strong/probable rows anchored to the order's supplier CNY payment can be confirmed idempotently, CNY-account fees create existing CNY ledger `bank_fee`/`transfer_fee` operations, RUB-account fees add directly to order bank fees, and supplier registry/order card expose `Точная себестоимость, ₽/шт` / `Себестоимость на единицу товара` without falling back to `approx_yuan_rate`.
37. `Поставки -> От поставщика -> Документы` поддерживает `Packing list / Упаковочный лист` как `packing_list` upload из `.xls/.xlsx`: parser extracts cartons, quantity, gross weight, volume, carton size and model summary into server-owned financial document payload; checklist includes `Комиссии банка` and `Packing list / Упаковочный лист` with red/yellow/green statuses, and delete/dedupe prevents duplicated `Оплата поставщику CNY` rows from linked CNY ledger documents.
38. Active cost truth с `2026-07-01` принадлежит functional warehouse/cost engine: frozen cost map from the proven accepted-on-FF shipment near 24.06 plus known downstream costs covers the permitted historical WB cost/Proxy 3 projection, while full warehouse history begins only at `warehouse_functional_cutover_v1`. `our_wb_unit_cost_rub`, Proxy 3, Finance/P&L, товарный капитал and direct consumers read this single projection; prior canonical-cost baseline/anomaly policies remain immutable migration audit;
39. `ФФ -> Остатки ФФ` добавлен как server-owned quantity ledger: manual Excel receipt/writeoff preview-confirm documents with original-file download, computed active-SKU balances with negative warnings, supplier accepted-on-ФФ auto receipts, WB statuses `3/4/5/6` auto writeoffs by goods composition only after explicit WB auto-writeoff checkpoint/baseline, ledger activation/opening balance and only when the debit does not make SKU balances negative, `Допринято` skip, and third calculation source `ff_stock_ledger` for factory-order and WB regional supply.
40. Для baseline-known WB-поставки `40561872` добавлен отдельный repo-owned targeted reconciliation runner: cache-only composition/current nomenclature, exact canonical source key, read-only v2 plan with per-SKU balances and stable fingerprint, bounded pre-activation remediation только для одновременных `wb_supply_before_auto_writeoff_checkpoint` + `wb_supply_before_ledger_activation`, explicit apply plus integrity-checked SQLite backup and atomic activation/checkpoint/balance recheck, idempotent linked `auto_writeoff`, post-run reconciliation and audited compensating receipt. Runner не доступен в UI, не поддерживает bulk/другие supply id и не меняет ordinary checkpoint/pre-activation/sync/backfill/detail semantics.
41. `Товарный капитал` is the single active projection of six mutually exclusive stages, each exposing only quantity/WAC/capital plus overall quantity/capital/WAC. Public quantity is physical quantity; paid-equivalent, coverage, confirmation, old 1C/Proxy 2, underaccepted and legacy inventory-return rows are technical archive. Overall WAC is total Decimal capital divided by total physical quantity, with no accepted-supply, discrepancy or WB-substate double count;
42. Legacy `CUTOVER_UNMATCHED_DOPRINATO_ABSORPTION_V1/V2/V3` manifests remain immutable audit evidence only. Active doprinato matching is pooled strictly by SKU against positive `Расхождения приёмки WB`; surplus enters transitional unmatched audit and never creates negative quantity/capital;
43. `Управление SKU` добавлено как самостоятельный section-authorized operator contour: canonical active-nmID table, explainable calculation-only deficit forecast over existing stock/supply/demand evidence, server-owned preferences, guarded inline seller-price and exact campaign/placement bid writes through existing WB adapters, advisory stabilization warnings, confirmed-readback action history and daily price/bid delta plus observed buyer-price metrics. ИИ, recommendations, auto/bulk control, real synthetic orders and production backfill остаются out of scope.
44. `Остатки` contains `Склады и себестоимость` and `Отчёт об остатках`. The active `warehouse_functional_cutover_v1` atomically publishes six-stage Decimal quantities/WAC/capital/quality, complete official WB contour snapshots, discrepancy/unmatched state, frozen historical cost projection and initial calculation parameters. `warehouse_opening_v1` remains unchanged audit. A separate repo-owned hourly/manual WB pipeline publishes coherent versions; the global vitrina refresh only consumes them.
45. Warehouse-chain recovery uses exact ready-snapshot date columns for 2026-07-01+ history, stable active-nomenclature `nm_id` identity, version-scoped unmatched audit ids and a guarded economics cutover that fills WB cost/Proxy 3 while removing only centrally enumerated archived public metrics. Warehouse UI localizes quality/evidence, keeps raw JSON secondary, shows sync time/reason and verifies dark action states in Playwright.
46. `Отзывы -> Отзывы` имеет production server autoanswers v1 contour: WB sync/backfill since `2026-01-01`, immutable owner-confirmed initial snapshot plus bounded rolling admission of new exact unanswered versions, literal live priority `content 1★ → 2★ → 3★ → 4★ → 5★ → rating-only` across processing and publication, schema-v8 exact per-sweep/member/version reconciliation acknowledgements with action-first selection and terminal/human-only barrier exclusion, truthful progress/stall telemetry, canonical semantic/observation versions, safe photo + HLS media with four deterministic frames, untouched frozen AI bundle `1.4.2`, bounded opaque Node retry with conservative per-attempt holds, `off/manual/draft_only/auto_safe/auto_all`, durable `policy_epoch` reconciliation, media-aware regeneration, server-validated persistent operator limits with confirmed readback, protected compact UI and mandatory readback publication state machine. A fingerprint-bound repo-owned recovery may add only missing preservation acknowledgements for an exact stalled sweep while keeping job/publication/readback/cost evidence immutable. A global budget increase is re-evaluated on the next worker tick without replacing the active transition run; its owner-confirmed run cap remains a separate immutable boundary. Feature settings are the sole business-intent owner; a mode mutation is successful only after the dedicated readonly/worker lifecycle is reconciled and read back. `Настройки -> Автообновления` monitors Autoanswers without an individual toggle, while its global switch remains a cross-system pause.
47. `Инструкции -> Ведение поставок` использует split typed content/registry/update records, integer revisions, append-only-in-practice нормализованную историю и server-side `NEW` на 30 canonical business days без localStorage/cookies/personal acknowledgement. Revision `2` добавляет section `Подбор складов WB по направлениям`: менеджер использует актуальный rank `#1` / `Рекомендуемый склад`, отдельно проверяет ЦФО Север/Восток/Юг и кабинет WB; статичное дублирование названий из warehouse registry запрещено, а system quantities пока не заменяют Excel allocation руководителя. Revision `3` добавляет заметный block `Точный состав поставки`: при создании поставки WB сразу указываются полный фактический список SKU, точное количество каждого SKU и правильное общее количество без временной условной SKU; состав используется далее при синхронизации и обработке движения товара для перемещения, списания остатков ФФ и себестоимости. Более поздний block получает собственный `NEW`, даже пока parent section остаётся `NEW` из предыдущей update-записи; blocks, добавленные вместе с новым section одной записью, дочерний badge не дублируют.
48. `Отчёты` содержит `Финотчёт ВБ` с единым canonical `Себестоимость WB наша` resolver на всей истории (до 01.07 — same-SKU projection exact 01.07, после — exact operation date), separate agent/acquiring, supply-layer-proven capitalization and compact business-quality expense arrows. Single-SKU `Партнёрский отчёт` uses server-owned settings, indexed Finance aggregate, root/nested accepted `ads_compact`, UI-first preview and matching light XLSX. Строка `Прочие прямые и распределённые расходы` раскрывается в четыре business-категории, при этом allocation coefficient/source digests остаются только internal evidence. Production acceptance fail-closed требует ready preview и semantic UI/XLSX reconciliation; active finalization/ZIP/raw Finance export are absent. Production recalculation runs only through hosted `finance-canonical-dry-run/apply/readback` with a new exact fingerprint, approval, backup and non-target invariants.
49. Runtime SQLite writers используют единый bounded contention contract с приоритетом интерактивных операций, sanitized wait/transaction observability и контролируемыми русскими `503 retryable`/`202 pending` вместо raw `database is locked`. Autoanswers worker/readonly sync физически изолированы в `wb_autoanswers_runtime.sqlite3` через проверяемую deploy migration, supplier confirmation preview — в отдельном confirmation store, а один bank-statement PDF хранится content-addressed по SHA-256 и связывается с поставками только через атомарные semantic-operation assignments. Подтверждение комиссии остаётся idempotent/revision-bound, одной транзакцией сохраняет expense/assignment/CNY-document rows и запускает только exact shipment/SKU/stage replay.

## Что не следует считать частью текущего `main`

- full legacy parity по всем историческим registry rows и metric sections;
- production storage binding для registry upload и broader user-management beyond current simple WebCore auth;
- configured GitHub Environment production credentials для автоматического live release; repo-owned wiring остаётся fail-closed до их binding;
- материализованные слои `packages/domain`, `infra/`, `tests/`, `api/`, `jobs/`, `db/`.

## Governance Pointers

Канонические governance/source-of-truth правила живут не в `README.md`, а в:
- [AGENTS.md](AGENTS.md)
- [docs/architecture/03_source_of_truth_policy.md](docs/architecture/03_source_of_truth_policy.md)
- [docs/architecture/07_codex_execution_protocol.md](docs/architecture/07_codex_execution_protocol.md)
- [docs/architecture/02_repo_workspace_blueprint.md](docs/architecture/02_repo_workspace_blueprint.md)
- [docs/architecture/10_hosted_runtime_deploy_contract.md](docs/architecture/10_hosted_runtime_deploy_contract.md)
- [docs/architecture/11_github_release_train.md](docs/architecture/11_github_release_train.md)

`README.md` остаётся summary/navigation entrypoint и не должен использоваться как самостоятельный carrier operational governance.

## Где смотреть детали

- [docs/modules/00_INDEX__MODULES.md](docs/modules/00_INDEX__MODULES.md)
- [docs/architecture/00_migration_charter.md](docs/architecture/00_migration_charter.md)
- [docs/architecture/01_target_architecture.md](docs/architecture/01_target_architecture.md)
- [docs/architecture/10_hosted_runtime_deploy_contract.md](docs/architecture/10_hosted_runtime_deploy_contract.md)
- [docs/architecture/11_github_release_train.md](docs/architecture/11_github_release_train.md)
- [migration/75_registry_v2_minimal_schema.md](migration/75_registry_v2_minimal_schema.md)
- [migration/76_metric_runtime_registry_minimal_schema.md](migration/76_metric_runtime_registry_minimal_schema.md)
- [migration/77_registry_implementation_path.md](migration/77_registry_implementation_path.md)
- [migration/78_pilot_registry_bundle.md](migration/78_pilot_registry_bundle.md)
- [migration/86_registry_upload_contract.md](migration/86_registry_upload_contract.md)
- [migration/87_registry_upload_bundle_v1.md](migration/87_registry_upload_bundle_v1.md)
- [migration/88_registry_upload_file_backed_service.md](migration/88_registry_upload_file_backed_service.md)
- [migration/89_registry_upload_db_backed_runtime.md](migration/89_registry_upload_db_backed_runtime.md)
- [migration/90_registry_upload_http_entrypoint.md](migration/90_registry_upload_http_entrypoint.md)
- [migration/91_sheet_vitrina_v1_registry_upload_trigger.md](migration/91_sheet_vitrina_v1_registry_upload_trigger.md)
- [migration/92_sheet_vitrina_v1_registry_seed_v3_bootstrap.md](migration/92_sheet_vitrina_v1_registry_seed_v3_bootstrap.md)
- [migration/93_sheet_vitrina_v1_mvp_end_to_end.md](migration/93_sheet_vitrina_v1_mvp_end_to_end.md)
- [docs/modules/45_MODULE__OWN_PRODUCT_CAPITAL_BLOCK.md](docs/modules/45_MODULE__OWN_PRODUCT_CAPITAL_BLOCK.md)
- [migration/98_own_product_capital.md](migration/98_own_product_capital.md)
- [migration/99_unified_canonical_cost_engine.md](migration/99_unified_canonical_cost_engine.md)
- [docs/modules/46_MODULE__SKU_MANAGEMENT_BLOCK.md](docs/modules/46_MODULE__SKU_MANAGEMENT_BLOCK.md) — active operator `Управление SKU`: canonical active-nmID table, calculation-only deficit forecast, server-owned preferences, guarded inline seller-price/exact-placement bid actions, stabilization warnings, confirmed event history and action/buyer-price metrics; no AI, auto/bulk control or real synthetic orders.
- [docs/modules/47_MODULE__OPERATOR_INSTRUCTIONS_KNOWLEDGE_BASE.md](docs/modules/47_MODULE__OPERATOR_INSTRUCTIONS_KNOWLEDGE_BASE.md) — protected repo-owned system section `Инструкции`: per-article structured content, revisions, normalized update history and 30-day server-side `NEW`, with server-owned `instructions` capability and no CMS/public/DOCX runtime surface.
- [docs/modules/48_MODULE__WAREHOUSE_STOCKS_BLOCK.md](docs/modules/48_MODULE__WAREHOUSE_STOCKS_BLOCK.md) — active six-warehouse quantity/WAC/capital engine, official WB contour snapshot, guarded functional cutover, targeted `2026-07-01+` WB cost/Proxy publication and canonical hosted dry-run/apply/readback/rollback commands.
- [docs/modules/49_MODULE__WB_AUTOANSWERS_SERVER.md](docs/modules/49_MODULE__WB_AUTOANSWERS_SERVER.md) — server-native WB feedback sync, five-state feature-owned lifecycle, content rating priority `1→2→3→4→5` before rating-only, frozen v1.4.2 guards, conservative budget reconciliation and durable readback-confirmed publication.
- [docs/modules/50_MODULE__PARTNER_REPORT_BLOCK.md](docs/modules/50_MODULE__PARTNER_REPORT_BLOCK.md) — server-owned one-card profitability with indexed UI preview, explicit blockers and source-digest-bound light XLSX; no active finalization/ZIP/raw export.
- [migration/102_warehouse_opening_snapshot.md](migration/102_warehouse_opening_snapshot.md) — bounded production initialization scope, schema, gates, reconciliation and recovery protocol.
- [migration/103_warehouse_functional_cutover.md](migration/103_warehouse_functional_cutover.md) — guarded six-stage functional apply, economics publication, hourly sync and production UI gate.
- [migration/104_warehouse_chain_audit_recovery.md](migration/104_warehouse_chain_audit_recovery.md) — exact-date historical recovery, archived metric cutover, nomenclature identity and UI acceptance contract.
- [migration/105_wb_autoanswers_server_v1.md](migration/105_wb_autoanswers_server_v1.md) — staged activation, external gates and fail-safe rollback for server autoanswers v1.
- [migration/107_wb_autoanswers_media_policy_epoch.md](migration/107_wb_autoanswers_media_policy_epoch.md) — additive schema v3, media canary, policy-epoch rollout and rollback posture.
- [migration/119_autoanswers_rolling_priority.md](migration/119_autoanswers_rolling_priority.md) — schema-v7 rolling admission, literal cross-stage priority, bounded Node exit handling and exact affected-row recovery.
- [migration/122_autoanswers_reconciliation_stall.md](migration/122_autoanswers_reconciliation_stall.md) — schema-v8 exact reconciliation acknowledgements, action-first recovery from preserved-row stalls and immutable execution-evidence contract.
- [migration/106_warehouse_cost_transparency_and_business_date_recovery.md](migration/106_warehouse_cost_transparency_and_business_date_recovery.md) — per-SKU supplier cost proof, version-scoped certification, exact business-date warehouse history and guarded derived recovery.
- [migration/107_finance_retro_cost_and_partner_report.md](migration/107_finance_retro_cost_and_partner_report.md) — superseded/revoked retro-cost and Partner ZIP migration identity; non-executable audit only.
- [migration/108_finance_canonical_cost_partner_ui_recovery.md](migration/108_finance_canonical_cost_partner_ui_recovery.md) — all-history canonical Finance dry-run/apply/readback and UI-first Partner Report recovery contract.
- [migration/109_warehouse_business_approved_archival_estimate.md](migration/109_warehouse_business_approved_archival_estimate.md) — exact 18-SKU owner-approved 100 ₽ archival WB cost basis, versioned source correction and guarded Finance recovery handoff.
- [migration/121_global_sqlite_contention.md](migration/121_global_sqlite_contention.md) — global bounded contention recovery, Autoanswers store split, durable bank preview/content-addressed source and exact targeted replay contract.
